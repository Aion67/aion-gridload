"""Dataset loading helpers."""

from __future__ import annotations

from pathlib import Path


def load_dataset(path: str | Path):
    """Load the project dataset from disk.

    Replace this stub with the actual loader once the dataset format is confirmed.
    """
    raise NotImplementedError("Implement dataset loading for the chosen file format.")